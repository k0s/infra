---
k0sngin_service_user: jhammel
k0sngin_service_top_level: "{{ ansible_env.HOME }}/web/site"
k0sngin_service_venv: "{{ ansible_env.HOME }}/src/k0sNgin/.venv"
# Where this repo (k0s/infra) is cloned on the host — links.json lives at its
# root and is the single source of truth shared with the `links` role.
k0sngin_service_infra_repo_path: "{{ ansible_env.HOME }}/src/infra"
